链表反转(头插法)
```cpp
ListNode* reverseList(ListNode* head){
    ListNode* tail = nullptr;
    while(head != nullptr){
        ListNode* next = head->next;
        head->next = tail;
        tail = head;
        head = next;
    }
    return tail;
}
